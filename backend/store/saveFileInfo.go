package store

import "github.com/image-generator/models"

// save all of the file info to the database

func SaveFileInfoToDB(userInfo models.UserInfo){
	// connect to the database and just store the userInfo
	
}