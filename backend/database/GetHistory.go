package database

import "github.com/image-generator/internal/models"


func GetHistory(userId int) (models.History){
	// based on the uploaded files get all of the data from the user
	return models.History{}
}